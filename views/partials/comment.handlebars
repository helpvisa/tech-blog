<!-- display a given comment -->
<article class="card m-2 w-">
    <!-- commenter info -->
    <div class="card-header font-italic">
        {{user.username}} // <span class="font-weight-bold">{{formatDate created_at}}</span>:
    </div>
    <div class="card-body">
        <p class="card-text">{{text}}</p>
    </div>
</article>